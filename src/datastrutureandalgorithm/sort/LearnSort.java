package datastrutureandalgorithm.sort;

import java.util.Arrays;

public class LearnSort {
    public static void main(String args[]){
        int number[]={12,34,3,5,7,8,29};
       Arrays.sort(number);
      //  System.out.println(Arrays.toString(number)); // This print line will show ascending value in array
        for (Integer in:number) {
            System.out.println(in+" ");
        }
        }
    }

